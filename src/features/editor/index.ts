/**
 * Editor feature module.
 *
 * The self-contained home for the visual map editor: marker placement,
 * dragging, property editing, preview, and undo/redo. Lazy-loaded only
 * after the user selects Edit (plan §3.1). Depends on services and
 * domain, never on providers directly.
 *
 * Placeholder for a later milestone. No implementation here.
 */

export {};
